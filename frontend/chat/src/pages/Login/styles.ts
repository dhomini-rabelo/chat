import styled from "styled-components";



export const Div = {
    or: styled.div`
      .line {
        height: 1px;
        background: var(--pBlack-300);
      }
      span {
        font-weight: 500;
        font-size: 14px;
        line-height: 17px;
        color: var(--pBlack-300);
      }
    `,
}


export const Button = {
    login: styled.button`
      background: #FFFFFF;
      box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      font-weight: 500;
      font-size: 16px;
      line-height: 19px;
      color: var(--pBlack-700);
    `,
}