import styled from 'styled-components'

export const Div = {
  chat: styled.div`
    border-block: 1px solid var(--pGray-200);

    strong {
      font-weight: 600;
      line-height: 1.062rem;
    }
  `,
}
